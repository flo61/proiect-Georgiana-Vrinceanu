@page
@model Geo.Pages.Trust.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Incredere[0].Whocansee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Incredere[0].Todolist)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Incredere[0].Todolist.Countries)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Incredere[0].ReturnDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Incredere) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Whocansee.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Todolist.Incredere.Todolist.Movies)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Todolist.Incredere.Todolist.Countries)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
